<Window x:Class="Zuzda.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zuzda" Height="400" Width="600"
        WindowStartupLocation="CenterScreen"
        KeyDown="Window_KeyDown"
        KeyUp="Window_KeyUp"
        ResizeMode="NoResize">

    <Window.Resources>
        <DropShadowEffect x:Key="ShadowEffect" Color="Black" Direction="320" ShadowDepth="3" Opacity="0.7" BlurRadius="4"/>
    </Window.Resources>

    <Canvas Name="GameCanvas">
        <Canvas.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#FF87CEFA" Offset="0"/>
                <GradientStop Color="#FF4682B4" Offset="1"/>
            </LinearGradientBrush>
        </Canvas.Background>

        <TextBlock Name="ScoreText" FontSize="24" FontWeight="Bold" Foreground="White"
                   Canvas.Left="10" Canvas.Top="10"/>

        <Rectangle x:Name="BorderRect" Canvas.Left="0" Canvas.Top="0" Stroke="White" StrokeThickness="4" Fill="Transparent"/>
    </Canvas>
</Window>
